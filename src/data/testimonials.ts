export interface Testimonial {
  quote: string;
  author: string;
}

// Yong will supply real testimonials later. None are listed here, so no
// Review or AggregateRating schema is generated (nothing invented).
export const testimonials: Testimonial[] = [];
